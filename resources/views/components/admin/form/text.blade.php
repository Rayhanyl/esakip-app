<div class="{{ $col }} mt-2">
    <label for="{{ $name }}" class="form-label">{{ $label }}</label>
    <input type="{{ $type }}" id="{{ $name }}" class="form-control" aria-describedby="{{ $name }}"
        name="{{ $name }}">
</div>
